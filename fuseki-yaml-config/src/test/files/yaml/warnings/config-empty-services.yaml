version: "1.0"
server:
  name: "Fuseki server 1"

services:

databases:
  - name: "mem-db"
    dbtype: TIM
  - name: "tdb2-db"
    dbtype: TDB2
    location: "DB2"
    settings:
      'tdb2:unionDefaultGraph': true
